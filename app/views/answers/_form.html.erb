<%= form_for @answer, url: "/questions/#{params[:id]}/answers" do |f| %>
	<%= f.text_area :body, class: "form-control", :size => "25x20"  %>
	 <%= f.submit class: "btn btn-default modal-button"%>
<% end %>
